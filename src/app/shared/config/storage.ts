'use strict';
import { ILocalStorageServiceConfig } from 'angular-2-local-storage';

export const SRPConfig:ILocalStorageServiceConfig = {
  prefix: 'srp-restoration-management',
  storageType: 'localStorage'
};
